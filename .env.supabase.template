VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key
VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-publishable-key

# Optional: service role key for server-side scripts (DO NOT expose in browser builds)
# SUPABASE_SERVICE_ROLE_KEY=

# Transactional notification webhook setup
# 1. Deploy the Netlify function at /api/send-banking-alert
# 2. Set NOTIFICATION_WEBHOOK_SECRET in Netlify
# 3. Run supabase/transactional_notification_webhooks.sql in the SQL editor:
#    select public.install_banking_notification_webhooks(
#      'https://your-netlify-site.netlify.app/api/send-banking-alert',
#      'your-shared-webhook-secret'
#    );
